/**
 * Convert Celsius to Fahrenheit
 *
 * @param {number} value
 * @returns number
 */
const convertCelciusToFahrenheit = (value) => {
    return (value * 9) / 5 + 32;
};

export default convertCelciusToFahrenheit;
